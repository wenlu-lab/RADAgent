[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "gbs-agent"
version = "0.1.0"
description = "Open-source agentic runtime for the GBS bioinformatics pipeline"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.40.0",
    "pydantic>=2.7.0",
    "pyyaml>=6.0",
    "click>=8.1",
]

[project.optional-dependencies]
test = [
    "pytest>=8.0",
    "pytest-mock>=3.12",
    "pytest-timeout>=2.3",
]

[project.scripts]
gbs-agent = "gbs.cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["gbs*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"
